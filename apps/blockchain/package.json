{
  "name": "oracle",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "dev": "npx hardhat node",
    "test": "npx hardhat test",
    "format": "npx prettier --write . --plugin=prettier-plugin-solidity 'contracts/**/*.sol'",
    "format-check": "npx prettier --check . --plugin=prettier-plugin-solidity 'contracts/**/*.sol'",
    "lint": "npx eslint . --ext .ts",
    "build": "npx hardhat compile",
    "deploy": "npx hardhat run scripts/deploy.ts --network localhost",
    "deploy-addressRecord": "npx hardhat run scripts/deploy-addressRecord.ts --network localhost"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^2.0.2",
    "@typescript-eslint/eslint-plugin": "^5.59.11",
    "@typescript-eslint/parser": "^5.59.11",
    "eslint": "^8.43.0",
    "eslint-config-prettier": "^8.8.0",
    "hardhat": "^2.17.2",
    "prettier": "3.0.3",
    "prettier-plugin-solidity": "^1.1.3"
  },
  "dependencies": {
    "dotenv": "^16.3.1"
  }
}
