{
  "name": "oracle",
  "version": "1.0.0",
  "description": "A blockchain oracle platform built with Hardhat and Solidity.",
  "license": "MIT",
  "private": true,
  "workspaces": ["packages/*", "apps/*", "docs/*"],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "format-check": "turbo run format-check",
    "format": "turbo run format"
  },
  "devDependencies": {
    "eslint-config-custom": "*",
    "eslint": "7.32.0",
    "prettier": "^2.5.1",
    "turbo": "^1.10.1"
  }
}
